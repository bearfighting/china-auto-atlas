# China Auto Atlas — Shared Source Registry

All structured sources live here, including sources that are specific to one domain and sources shared across domains.

Every source ID must be globally unique across `data/`. The loader should index every YAML file in this directory into one
source registry at build time.
