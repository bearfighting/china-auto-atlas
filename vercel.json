{
  "buildCommand": ". .venv/bin/activate && pnpm data:validate && pnpm data:build && pnpm typecheck && pnpm lint && next build",
  "installCommand": "pnpm install --frozen-lockfile && python3 -m venv .venv && .venv/bin/python -m pip install -r requirements-dev.txt",
  "framework": "nextjs"
}
